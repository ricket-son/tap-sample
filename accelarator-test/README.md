# Kubeconfig Accelerator

This project contains just a useless kubeconfig generator that replaces the users name, namespace and token to access a non existing kubernetes cluster.

The created values:

- USER_NAME
- USER_TOKEN
- USER_NAMESPACE
